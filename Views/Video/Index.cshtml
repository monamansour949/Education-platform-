@model IEnumerable<ProjectItiTeam.Models.Video>

@{
    ViewData["Title"] = "Videos Course";
}
@if (User.IsInRole("Admin"))
{
    <div class="row " style="margin-top: 1%;text-align: left;">
        <div class="col-lg-12 col-md-12 my-4 text-center">
            <a href="/Video/New" class="btn btn-info form-control w-50 text-light"><strong>Add Video</strong></a>
        </div>
    </div>
}

<div class="row marginTop row">
    @foreach (var video in Model)
    {
         <div class="p-2 box-shadow rounded border col-md-4 col-12">
            <video controls width="100%" height="100%">

            <source src="@video.pathViedoes" type="video/mp4">
            <source src="@video.pathViedoes" type="audio/ogg">
            </video>
          </div>
        @Html.Partial("_VideoSummary", video)
    }
</div>




@*<h1>Browse Videos Course</h1>



<table class="table table-striped">
    <thead class="thead-dark">
        <tr>
            <td>@Html.DisplayNameFor(m => m.ID)</td>
            <td>@Html.DisplayNameFor(m => m.Name)</td>
            <td>@Html.DisplayNameFor(m => m.Description)</td>
            <td>@Html.DisplayNameFor(m => m.Course_ID)</td>
            <td>@Html.DisplayNameFor(m => m.User_Id)</td>
            <th>Links</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Video g in Model) 
        {
            <tr>
                <td>@Html.DisplayFor(m => g.ID)</td>
                <td>@Html.DisplayFor(m => g.Name)</td>
                <td>@Html.DisplayFor(m => g.Description)</td>
                <td>@Html.DisplayFor(m => g.Course_ID)</td>
                <td>@Html.DisplayFor(m => g.User_Id)</td>
                <td>
                    <a asp-action="Update" asp-route-id="@g.ID" class="btn btn-secondary">Edit</a> | <a asp-action="Delete" asp-route-id="@g.ID" class="btn btn-danger">Delete</a> | 
                    <a asp-action="Add" asp-controller="Cart" asp-route-id="@g.ID" class="btn btn-primary">Add to cart</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<nav aria-label="Catalog Pages">
    <ul class="pagination">
        @for (int i = 1; i <= (int)ViewData["Pages"]; i++) //This displays the total available pages in the video library
        {
            if (i == (int)ViewData["CurrentPage"])
            {
                <li class="page-item active" aria-current="page">
                    <a class="page-link" href="#">
                        @i <span class="sr-only">(current)</span> @* This highlights the current page in the video library 
                    </a>
                </li>
            }
            else
            {
                <li class="page-item">
                    <a class="page-link" asp-action="Index" asp-route-id="@i">
                         Page number in the route 
                    </a>
                </li>
            }
        }
    </ul>
</nav>*@